apiVersion: v1
kind: Service
metadata:
  name: postgresql-service
  namespace: invoice-generator-namespace
spec:
  selector:
    app: postgresql
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP